#include<bits/stdc++.h>
using namespace std; 

int main() 
{
    vector<vector<string>> s;
    vector<vector<string>> p;
    for(auto &t: s){
        cout<<"hello";
        t.insert(t.begin(),"hello");
        p.insert(p.begin(), t);
    }
}